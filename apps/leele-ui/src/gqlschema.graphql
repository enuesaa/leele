schema {
  query: Query
  mutation: Mutation
}

type Mutation {
  createNote(message: String!): ID!
}

type Note {
  id: ID!
  message: String!
}

type Query {
  note(id: ID!): Note
  notes: [Note!]!
}
