type Note {
	id: ID!
	message: String!
}

type Mutation {
	createNote(message: String!): ID!
}

type Query {
	notes: [Note!]!
	note(id: ID!): Note
}

type Subscription {
	noteCreated: Note!
}

schema {
	query: Query
	mutation: Mutation
	subscription: Subscription
}